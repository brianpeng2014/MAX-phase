# MAX-phase 研究專案

本專案用於 Ti3SiC2 MAX phase 快速燒結與熱性質研究。全部研究輸出使用繁體中文。

## 開工順序

1. 先讀「研究側寫.md」。
2. 研究規劃以「docs/research/碩論架構規劃書.md」為唯一正文。
3. 撰寫正式內容前讀「寫作準則.md」。

## 架構基準

- 根目錄 `README.md` 是目前與未來專案結構的主要參考，架構可隨研究進展迭代。
- README 中尚未建立、尚無內部資料或暫時失效的連結屬於規劃入口，不因目前不可用而自行刪除。
- 新增目錄、工具或協作流程時，優先沿用 README 的責任分層；若必須偏離，先同步更新 README。

## 資料分層

- library/manifest.csv 是文獻狀態的唯一來源；PDF 原檔放 library/pdf 且不改檔名。
- 儀器原始檔只放 data/raw，不得修改；分析程式、結果與圖分開存放。
- 正式稿件與書目放 thesis，交付檔放 thesis/out。
- 數值換算、量測命名與重要取捨記錄在 decisions。
- _legacy 只供人工查閱，不主動讀寫。

## Git 與 PR 交付

- 每次新增檔案或變更資料／文件，按一個完整工作批次建立新分支及新的 GitHub PR，目標為 `main`。
- 完成變更與必要驗證後提交、推送並開 PR，無須再次詢問是否開 PR；不直接推送 main，合併由使用者決定。
- 同一批次的修正更新該 PR；下一個獨立批次另開新 PR。尚未合併時確認分支依賴，避免夾帶無關變更。
- 本機依賴、預覽圖與檢查暫存檔不提交；需交付的研究文件、分析程式及 Excel 納入版本管理。

## 研究準則

1. 從數據與文獻推導結論，不發明或改動量測值、樣本數與單位。
2. 預設不做推論統計；可以報告直接比較、差值、倍率、平均、範圍與換算。
3. 文獻主張進入正文前，必須回查原始 PDF 與頁碼。
4. 數字應附一次來源，例如頁碼、資料檔或分析腳本。
5. 不修改儀器原始資料，不刪除追蹤檔，不 force push。
6. 僅執行與當前需求相關的驗證，回報保持精簡。
